# [START runtime]
runtime: nodejs
env: flex
# [END runtime]

# [START handlers]
handlers:
 - url: /
   static_files: build/index.html
   upload: build/index.html

 - url: /(.*)
   static_files: build/\1
   upload: build/(.*)
# [END handlers]

env_variables:
  DOMAIN: 
  GOOG_CLIENT_ID:
  GOOG_CLIENT_SECRET:
  MEMCACHE_URL:
  MEMCACHE_USERNAME:
  MEMCACHE_PASSWORD:
  PROJECT_ID:
  BUCKET_NAME_SALT:
  SESSION_SECRET:
